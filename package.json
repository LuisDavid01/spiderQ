{
  "name": "spiderq",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
	  "tui": "tui",
	  "dev-cli": "cd apps/cli-client && bun run dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}

